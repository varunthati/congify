<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Submission Result</title>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to right, #e0f7fa, #ffffff);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
      padding: 20px;
    }

    .result-card {
      background: #fff;
      padding: 2rem;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 600px;
      transition: transform 0.3s ease;
    }

    .result-card:hover {
      transform: scale(1.02);
    }

    .submissions-list {
      margin-top: 20px;
    }

    .submissions-list li {
      padding: 6px 0;
      border-bottom: 1px solid #eee;
    }

    .btn-home {
      margin-top: 20px;
      transition: all 0.3s ease;
    }

    .btn-home:hover {
      transform: scale(1.05);
    }
  </style>
</head>
<body>

  <div class="result-card">
    <h2 class="text-center mb-4 text-success">Submission Successful 🎉</h2>

    <p><strong>Name:</strong> <%= username %></p>
    <p><strong>Email:</strong> <%= email %></p>

    <div class="submissions-list">
      <h5 class="mt-4 mb-2">All Submissions:</h5>
      <ul class="list-unstyled">
        <% submissions.forEach((item, index) => { %>
          <li><%= index + 1 %>. <strong><%= item.username %></strong> - <%= item.email %></li>
        <% }) %>
      </ul>
    </div>

    <a href="/" class="btn btn-outline-primary w-100 btn-home">Submit Another</a>
  </div>

</body>
</html>
